/**
 * The CRM workspace is a single tabbed surface. Keep every tab on the same
 * working canvas so navigation and content boundaries do not jump between tabs.
 */
export const CRM_WORKSPACE_PAGE_VARIANT = "detailWide" as const;

export const CRM_WORKSPACE_TABLE_LAYOUT = {
  maxWidth: "2xl" as const,
};

export const CRM_WORKSPACE_SPLIT_PAGE_CLASSNAME =
  "mx-auto w-full max-w-screen-2xl";
