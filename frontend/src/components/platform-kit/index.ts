// Contained vendor drop of the Supabase UI "Platform Kit" (platform-kit-nextjs
// registry, https://supabase.com/ui/docs/platform/platform-kit). Kept as a
// single self-contained tree so it stays updatable from the registry and is
// excluded from the design-system lint gates (see eslint.config.mjs). Do not
// hand-edit these files for styling — restyle at the mount site instead.
export { default as SupabaseManagerDialog } from "./components/supabase-manager";
