import { ContextualHelpPrototype } from "@/features/knowledge/contextual-help-prototype";

// PROTOTYPE: Three contextual-learning patterns for the Prime Contract workflow,
// switchable with ?variant=annotated|walkthrough|explain on this throwaway route.
export default function PrimeContractHelpPrototypePage() {
  return <ContextualHelpPrototype />;
}
