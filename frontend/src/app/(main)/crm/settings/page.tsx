/* eslint-disable design-system/require-page-shell -- Feature-owned PageScaffold owns the page shell. */

import { CrmSettingsReview } from "@/features/crm/settings-review";

export default function CrmSettingsPage() {
  return <CrmSettingsReview />;
}
