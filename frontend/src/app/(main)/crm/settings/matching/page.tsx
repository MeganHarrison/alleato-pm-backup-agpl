/* eslint-disable design-system/require-page-shell -- UnifiedTablePage owns the page shell. */

import { CrmMatchingReview } from "@/features/crm/matching-review";

export default function CrmMatchingPage() {
  return <CrmMatchingReview />;
}
