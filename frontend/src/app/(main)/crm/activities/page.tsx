/* eslint-disable design-system/require-page-shell -- UnifiedTablePage owns the page shell. */

import { CrmActivitiesReview } from "@/features/crm/activities-review";

export default function CrmActivitiesPage() {
  return <CrmActivitiesReview />;
}
