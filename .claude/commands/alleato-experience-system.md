---
description: Calibrate, plan, implement, or review an Alleato interface with the canonical Experience System
argument-hint: "[calibrate|plan|implement|review] [route, feature, file, image, or design brief]"
---

Load and follow `.claude/skills/alleato-experience-system/SKILL.md` completely.

Apply it to:

$ARGUMENTS

If no mode is supplied, infer `calibrate`, `plan`, `implement`, or `review` from the request. For visual redesigns, calibrate from the existing artifact and references before planning. For review mode, inspect the running route when available and cite concrete evidence. For implementation, verify the canonical existing owner before writing UI, then render and iterate from screenshots. Always apply the Alleato product noise gate before closeout.
