{
  "contractVersion": 1,
  "taskId": "ALL-11-SCOPE-MIGRATION",
  "riskLevel": "high",
  "feature": "Existing mapped scope migration primitive",
  "route": "ProjectAssigner.assign_scope",
  "flows": [
    {
      "name": "Migrate persisted fake-project attribution",
      "steps": [
        "Resolve the existing project",
        "Read the canonical Business Area mapping",
        "Return a Business Area-only AssignmentTarget"
      ],
      "expectedOutcome": "Mapped containers migrate only when explicitly requested; real projects and default callers are unchanged"
    }
  ],
  "requiredEvidence": ["regressionTest", "independentReview"],
  "independentReview": { "required": true }
}
