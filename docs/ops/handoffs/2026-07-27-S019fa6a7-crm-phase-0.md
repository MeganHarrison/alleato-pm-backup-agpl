# CRM Phase 0 Handoff

Status: In Progress - Phase 1 blocked
Session: S019fa6a7
Task: ALL-40
Date: 2026-07-27

## Scope

This session owns only:

- `docs/ops/tasks/2026-07-27-crm-phase-0.md`
- `docs/ops/handoffs/2026-07-27-S019fa6a7-crm-phase-0.md`

No product code, database migrations, permission changes, provider changes, seed writes, or deployments are authorized in Phase 0.

## Current State

- Brandon approved starting the project.
- CRM Build Plan v4 limits that approval to Phase 0 discovery and task setup.
- Linear issue ALL-40 is assigned to Brandon and In Progress.
- The isolated workspace started from current `origin/main` at `a2a26834579408678821577980456e7561528213`.
- The plan's baseline, `51c2214136d7c5fab8ad2fb90b18b723bc1d61c0`,
  is stale. Repository and live database anchors were reverified.
- No product code, database schema/data, permission, provider configuration,
  seed data, or deployment state was changed.

## Work Completed

- Read the v4 Markdown plan and structurally inspected the v4 Word document.
- Confirmed the Word document has one title, 20 level-two headings, 49 level-three headings, 23 tables, and no tracked insertions/deletions.
- The bundled LibreOffice renderer was unavailable. A hidden Microsoft Word export also failed and the Word process created by the attempt was stopped; visual document QA is therefore not being represented as complete.
- Registered the canonical checkout and created an isolated task workspace.
- Created Linear issue ALL-40.
- Confirmed current shared frontend gates/primitives and all named package
  scripts.
- Queried the linked PM APP database read-only and recorded schema, row-count,
  duplicate, task-status, timezone, and document-identity evidence.
- Confirmed company create/update APIs enforce the ERP-master boundary.
- Confirmed task deletion is currently hard delete, not the v4 plan's assumed
  soft-delete flow.
- Confirmed CRM deal attachments need a project-independent scope decision.
- Confirmed cross-source communication deduplication is only partial.
- Attempted the required browser-auth verifier and recorded its Windows,
  Playwright, and provider-env failures.

## Critical Finding

The live database has no CRM relations, but current main still contains the
previous CRM APIs/components and a dashboard query for `crm_deals`. Those
surfaces point at tables deliberately dropped by
`20260724140000_drop_crm_from_pm_app.sql`.

Phase 1 must start with an approved retire-or-replace inventory and a guardrail
against dropped-table access. Treating the entire codebase as greenfield would
be unsafe.

## Next Actions

1. Obtain Brandon's business and architecture decisions listed in the task.
2. Repair and pass the production browser-auth verifier.
3. Verify the live Render cron configuration, or explicitly defer scheduled
   work.
4. Approve the stale CRM retire-or-replace inventory.
5. Begin Phase 1 only under a new implementation approval and task contract.

## Known Blockers

- Business decisions are pending: administrators, seed owners/sources,
  thresholds, USD-only acceptance, privacy approver, and pilot cohort.
- Architecture decisions are pending: task archive/unlink behavior,
  pre-conversion deal attachment scope, and reporting timezone source.
- Production browser authentication is not yet proven.
- Live Render scheduling is not yet proven.
- These blockers do not invalidate the completed read-only discovery, but they
  block Phase 1 approval.

## Failure-Loudly Notes

- Any access or verification failure must name the exact command/query, observed result, owner surface, and smallest recovery action.
- No stale plan statement will be treated as current without dated repository or database evidence.
- No secret should be copied into this task to bypass the browser verifier.
