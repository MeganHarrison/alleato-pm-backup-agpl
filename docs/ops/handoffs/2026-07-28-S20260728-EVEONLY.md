# Handoff: 2026-07-28 — Eve-only assistant runtime

## Intake Block

1) Session ID: S20260728-EVEONLY
2) Task ID: AAI-1265-EVE-ONLY
3) Linear issue: AAI-1265
4) Linear URL: https://linear.app/megankharrison/issue/AAI-1265
5) Current status: Complete and published
6) Files changed: See `git status --short`; all changes belong to the Eve-only cutover.
7) Commands run and outcome: Eve guard pass; 85 frontend focused tests pass; 5
   runtime gates pass; 39 agent tests pass; 24 protocol cases / 192 gates pass;
   32 tool-registry tests pass; Eve typecheck pass; authenticated browser flow pass.
8) Evidence artifact:
   `docs/ops/tasks/evidence/AAI-1265-eve-risk-answer.png` and
   `docs/ops/tasks/evidence/AAI-1265-ask-alleato-stop.png` and
   `docs/ops/tasks/evidence/AAI-1265-published-main-smoke.png`
9) Top findings:
   - The previous design kept a complete legacy generator beside Eve, so runtime
     ownership remained ambiguous and failure recovery silently depended on old code.
   - Eve now owns `/ai`, Ask Alleato transport, skill selection, tool selection,
     streaming, and answer generation through one agent root.
   - Durable out-of-band cancellation is not implemented by Eve 0.22.6; active
     stream stop works, while unsupported durable cancellation fails explicitly
     with `EVE_DURABLE_CANCEL_UNAVAILABLE` instead of lying about state.
10) Recommended next action: None for AAI-1265.
11) Handoff file:
    `docs/ops/handoffs/2026-07-28-S20260728-EVEONLY.md`
12) Migration ledger evidence: N/A — no database migration.

## Linear Updates

- Kickoff comment: Existing AAI-1265 scope supplied by the user.
- Milestone comments: Not posted; implementation evidence is consolidated here.
- Completion comment: Pending publication.

## Current Status

The legacy `/api/ai-assistant/chat` generator, runtime selector, executive agent
files, orchestrator, bot core, parity/canary scaffolding, duplicate Eve lab,
obsolete route namespace, stale eval fixture, and legacy-only verification
scripts are deleted. Root startup launches `agents/alleato-assistant`.

Authenticated browser validation at `http://localhost:3012/ai` proved a real
portfolio-risk question, two Eve tool POSTs, a correct continuation, persistence,
and restoration after reload.

## Exact Next Step

None. The Eve-only cutover is published and independently reviewed.

## Known Pitfalls

- Do not restore old `/ai-assistant` bookmarks or a fallback route; the requested
  cutover intentionally makes removed paths return 404.
- Eve 0.22.6 has no out-of-band durable cancel protocol. Do not mark an accepted
  turn canceled unless the runtime confirms it.
- The repo's frontend typecheck wrapper invokes Unix `rm` and fails on Windows
  before TypeScript starts. Focused changed-boundary tests and agent compilation
  are green; wrapper portability belongs to `frontend/package.json`, currently
  owned by another active workspace.

## Resume Commands

```powershell
cd C:\Users\KimiClaw\.codex\isolated-workspaces\s20260728-eveonly-aai-1265-eve-only-b757d1
npm run verify:eve-only-runtime
agent-browser --session eveonly open "http://localhost:3012/ai?session=42295220-c9cf-4f45-9e6e-b29509cb00b0"
```

## Evidence

- `docs/ops/tasks/AAI-1265-EVE-ONLY.md`
- `docs/ops/tasks/evidence/AAI-1265-eve-risk-answer.png`
- `docs/ops/tasks/evidence/AAI-1265-ask-alleato-stop.png`
- `docs/ops/tasks/evidence/AAI-1265-published-main-smoke.png`
- `docs/ops/tasks/evidence/AAI-1265-independent-review.txt`
- `docs/ops/tasks/evidence/AAI-1265-verification-manifest.json`
- `docs/ops/tasks/evidence/AAI-1265-verification-result.json`
- `%TEMP%\alleato-frontend-local.log`
- `%TEMP%\alleato-assistant-eve-local.log`
