# Handoff: 2026-07-22 - ALL-5 Schedule Resources and Assignments

## Intake Block

1) Session ID: S217
2) Task ID: ALL-5
3) Linear issue: https://linear.app/alleato-group/issue/ALL-5/build-phase-4a-schedule-resources-assignments-and-allocation
4) Current status: Complete - Pushed. Migration is applied, authenticated E2E and negative probes pass, independent reviews approve, and the task-owned revision is published to `origin/main`.
5) Ownership: person-resource schema, many-to-many assignments, revision resource/assignment snapshots, guarded assignment boundary, allocation diagnostics, canonical task editor/resource-load UI, focused tests, evidence, and closeout control plane.
6) Next action: begin Phase 4B with resource calendars/capacity exceptions and a preview-only leveling contract; update ALL-5 manually because browser policy blocked Linear access.

## Verification Contract

Verification contract: Required

- Focused engine, service, route, and component tests must pass.
- The additive migration must be generated by the Supabase CLI, applied to the linked project, and match the remote migration ledger.
- Readback must prove same-project constraints, active-member validation, grants/RLS, manager-only atomic writes, snapshot count equality, and snapshot immutability.
- Authenticated desktop/mobile canonical schedule proof must show assignment editing and visible allocation/overallocation diagnostics without task-date movement.
- Independent TypeScript/React, database, and final code reviews must approve the delivered revision before publication.

## Product Noise Gate

- Primary user/job/decision: project manager / assign project people to tasks / identify capacity conflicts before moving dates.
- Tier 1: task assignments and visible overload state.
- Tier 2: compact allocation percentages and daily resource load.
- Tier 3: diagnostics and legacy-data review warnings.
- Hidden by default: resource-load panel.
- Removed: parallel schedule/resource page, automatic leveling, and inferred legacy allocations.
- Primary action: save a task's assignment set atomically.
- Failure loudly: authorization, eligibility, malformed allocation, data-loading, and allocation-engine gaps are visible and actionable.

## Architecture Boundary

- `people` owns identity; `project_directory_memberships` owns project eligibility.
- `schedule_resources` materializes eligible project people; `schedule_task_assignments` solely owns canonical assignments.
- Legacy task assignee fields are warning-only and never dual-written.
- The existing project calendar supplies working days; Phase 4A assumes 100 percent daily capacity and never writes task dates.
- Revision snapshots capture resource and assignment facts in the same atomic transaction as task/dependency/calendar facts.

## Evidence

- Clean isolated workspace: `C:\Users\Brandon\Documents\Codex\iw\s217-all-5-7b0294`.
- Base commit: `11e636e71` from refreshed `origin/main`.
- Supabase April/May 2026 breaking change reviewed; the migration uses explicit grants in addition to RLS and fixed-search-path security-definer functions.
- Linked migration `20260722121917` applied and read back: four RLS tables, authenticated SELECT, service-role live-table DML/snapshot SELECT, composite tenant constraints, FK indexes, guarded RPC, count-equality snapshot integration, and two immutability triggers per snapshot table.
- Rollback-only linked mutation probes passed for direct DML, non-manager access, an actual out-of-project task, actual inactive person and inactive membership records, and owner-level immutable-trigger update/truncate attempts. Raw consolidated results are committed beside the SQL.
- Exact linked database generation and the manual table-type override gate pass. Live schema readback proved the former `submittal_documents` and submittal AI-review cache columns are absent, so their stale runtime compatibility accesses were removed.
- Focused coverage passed: eight Phase 4A suites / 33 tests, including deterministic resource and task-hierarchy pagination beyond 500 rows and chunked people lookups; five existing task-modal suites / 11 tests; and the post-review engine/editor/panel subset / 15 tests.
- Final authenticated Playwright workflow passed in 3.3 minutes on a clean cold-started server. It showed 150-percent load, 50-percent overallocation, task-modal save wiring, unchanged dates, captured resource/assignment snapshot counts, rejected snapshot writes, and complete cleanup.
- Desktop/mobile resource-load and task-assignment screenshots are in `docs/ops/evidence/2026-07-22-schedule-resources/` and were visually inspected.
- Targeted ESLint reports zero errors. A final full-repo TypeScript attempt emitted no diagnostics before its bounded 15-minute termination at roughly 4.5 GB working memory.
- Route build contracts pass. Direct Next production builds at 7 GB and 12 GB were resource-limited during repository-wide compile; neither emitted a task-owned source or TypeScript diagnostic.
- Independent React/UI, TypeScript/JavaScript, final code, and final database reviews approve after all task-owned findings were repaired. Final code re-review verified canonical specification paths and 501-task hierarchy pagination; database review independently reran the exact schema gates, service/API tests, linked readback, and rollback probes.

## Linear Update

- Issue `ALL-5` remains open at handoff because the active browser security policy explicitly rejected use of `linear.app`. No alternate browser, API, or indirect workaround was attempted.

## Deferred Boundary

- Automatic/manual leveling date writes: wait for preview, confirmation, audit, and undo contracts.
- Equipment/material resources, variable capacities, resource calendars/shifts, fixed work/units/duration, rates/cost/earned value, and Microsoft Project resource round-trip remain follow-on phases.
- S216 Phase 3 browser evidence remains independently pending and is not part of S217 ownership.
- Snapshot capture extends the existing global `SHARE`-lock pattern, so a project-scoped consistency design remains operational follow-up before high-concurrency usage.
- Monolithic Next build and TypeScript verification remain memory/time constrained on this workstation; focused tests, exact schema gates, targeted lint, browser proof, and independent reviews are the release evidence for this increment.
