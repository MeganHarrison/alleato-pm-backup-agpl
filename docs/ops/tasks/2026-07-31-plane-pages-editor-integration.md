# Task: Integrate Plane Pages Block Editor

Status: Ready for Integration
Owner: Codex S20260731-plane-pages-integration
Created: 2026-07-31
Task ID: AAI-plane-pages-integration
Linear Issue: AAI-1288 program slice; no new Linear issue requested
Related Handoff: generated by isolated-session-workspace at closeout

## Objective

Replace the legacy plain-text Pages detail editor with the Plane-derived block editor while preserving the existing secure, project-scoped `notes` API for reads and writes.

## Scope

- `frontend/src/features/plane-pages-editor/**`
- `frontend/src/features/plane-pages/**`
- This task record
- Excludes database migrations, production deployment, and persistent comments/version-history storage

## Source of Truth

- Canonical runtime/data owner: `frontend/src/app/api/notes/route.ts`
- Existing shared primitives/services: `frontend/src/features/plane-pages/plane-pages-data.ts`, `frontend/src/components/ui/**`
- Deprecated or parallel paths: the legacy `PlanePageEditor` composition is replaced only inside the Plane Pages workspace
- Required release-base dependency: `071cf112da454b2a50e5712887dea4674c3cc12a` (`Secure Plane Pages data access`)

Delivery lane: Standard

Verification contract: Optional

## Acceptance Criteria

- [x] Opening a project Page loads it through the existing project-scoped notes API.
- [x] Saving writes `projectId` and `pageId` through the existing permission-guarded notes API.
- [x] Block type and checked state survive a save/load round trip.
- [x] Comments and version history are disclosed as unavailable rather than simulated.
- [x] Save/load failures retain edits and provide an actionable recovery path.
- [x] Existing list, create, archive, search, and sort behavior remains owned by the Pages workspace.

## Implementation Checklist

- [x] Files/modules to change are listed before integration edits.
- [x] The existing notes API and `updateProjectPage` data owner are reused.
- [x] Errors are specific and actionable.
- [x] No schema, migration, or production state is changed.

## Integration and Verification

- [x] Focused Pages/editor unit tests pass.
- [x] Targeted lint passes.
- [x] Diff and ownership checks pass.
- [x] Local commit created by this handoff; publication is intentionally deferred to the release owner.

## Failure-Loudly Contract

- Cause surfaced as: a specific load/save error banner that preserves the current editor document.
- Detection path: focused adapter/editor tests and the Pages route UI.
- Recovery path: retry Save, return to Pages, or request persistent collaboration capability implementation.

## Incident Learning

- Failure fingerprint: `N/A`
- Root cause: N/A
- Detection gap: N/A
- Prevention: N/A
- Guardrail evidence: focused adapter and editor capability tests

## Evidence

| Check              | Command / artifact                                                                                                                                                                                                | Result | Notes                                                                      |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------- |
| Editor and adapter | `vitest run --config src/features/plane-pages-editor/vitest.config.ts --pool threads --maxWorkers 1 --no-file-parallelism`                                                                                        | Pass   | 3 suites, 18 tests; includes in-flight save and dirty-archive regressions. |
| Pages owner        | `npm.cmd run test:unit -- --runInBand src/features/plane-pages/plane-pages-data.unit.test.ts src/features/plane-pages/plane-pages-utils.unit.test.ts src/features/plane-pages/plane-pages-template.unit.test.tsx` | Pass   | 3 suites, 14 tests.                                                        |
| Targeted lint      | `eslint.cmd` on task-owned Pages/editor files                                                                                                                                                                     | Pass   | No lint findings.                                                          |
| Independent review | Reviewer re-review after two data-loss fixes                                                                                                                                                                      | Pass   | No remaining findings.                                                     |
| Diff hygiene       | `git diff --check`                                                                                                                                                                                                | Pass   | No whitespace errors.                                                      |

## Remaining Risk

- The production `notes` RLS migration remains unapplied and is outside this slice. Release must remain blocked until the release owner applies and verifies it with explicit user approval.
- Comments and version history have no persistent API; the production adapter advertises both capabilities as unavailable.

## Final Status

- [x] All implementation and verification checklist items within this non-publishing slice are complete.
- [x] Evidence is filled in.
- [x] Incident learning is explicitly N/A.
- [x] Deferred persistent collaboration features identify their missing owner boundary.
