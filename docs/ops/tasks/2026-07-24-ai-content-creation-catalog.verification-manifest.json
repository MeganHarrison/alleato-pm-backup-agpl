{
  "contractVersion": 1,
  "taskId": "AI-CONTENT-CREATION-CATALOG",
  "feature": "AI content-creation feature catalog",
  "route": "Supabase public.procore_features",
  "riskLevel": "high",
  "flows": [{
    "name": "Apply and read back the AI creation catalog",
    "steps": ["Apply the atomic migration", "Verify the migration ledger", "Read back implementation counts and both new columns"],
    "expectedOutcome": "Twelve implemented and seven unavailable AI creation workflows are present with screenshot and workflow metadata."
  }],
  "requiredEvidence": ["databaseReadback", "regressionTest", "screenshots"],
  "visual": { "viewports": ["desktop"] },
  "independentReview": { "required": true },
  "claims": [
    { "id": "remote-catalog", "description": "The applied database schema and rows match the requested catalog contract.", "evidence": [{ "key": "databaseReadback", "min": 1 }] },
    { "id": "migration-guardrail", "description": "The catalog seed only uses the live constrained complexity vocabulary.", "evidence": [{ "key": "regressionTest", "min": 1 }] },
    { "id": "assistant-reference", "description": "Implemented catalog rows reference an existing assistant UI screenshot rather than a mock.", "evidence": [{ "key": "screenshots", "min": 1 }] }
  ]
}
