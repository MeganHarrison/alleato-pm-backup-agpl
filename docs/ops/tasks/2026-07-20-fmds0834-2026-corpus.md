# Task: Stage FMDS 8-34 April 2026 Corpus

Status: Complete
Owner: Megan Harrison
Created: 2026-07-20
Task ID: AAI-1184
Linear Issue: [AAI-1184](https://linear.app/megankharrison/issue/AAI-1184/stage-and-vectorize-fmds-8-34-april-2026-corpus)
Related Handoff: Deferred; this session owns the isolated ASRS corpus slice.

## Objective

Create a complete, revision-isolated, retrieval-ready FMDS 8-34 April 2026 corpus in the dedicated ASRS Supabase project without deleting, blending, or activating over the 2024 corpus.

## Scope

- Owned source: `/Users/meganharrison/Downloads/FMDS0834 - 2026.pdf` (122 pages, Interim Revision April 2026).
- Owned database surface: revision, page, chunk, table, table-cell, figure, rule-card, ingestion-run, and activation-control records in ASRS Supabase `vqnnvpnoitqhijkztyhq`.
- Owned implementation: `scripts/asrs/` ingestion and verification scripts plus a separately owned ASRS database migration artifact.
- Explicit exclusion: deletion of 2024 data, activation of the 2026 revision, PM APP environment cutover, chat-route changes, and treating model-generated extraction as authoritative without review.

## Source of Truth

- Canonical document: the attached PDF, SHA-256 fingerprinted at ingestion.
- Canonical runtime/data owner: dedicated ASRS Supabase project `vqnnvpnoitqhijkztyhq`.
- Existing shared primitives/services: `backend/src/services/pipeline/vision_analyzer.py`, `backend/src/services/integrations/azure/document_intelligence.py`, `backend/src/services/pipeline/document_parser.py`.
- Deprecated or parallel paths: legacy revision-specific `fm_*` and `asrs_*` tables remain preserved for comparison but are not the canonical 2026 corpus contract.

Verification contract: Required

## Acceptance Criteria

- [x] One immutable `2026-04` revision is staged with the exact source hash and 122-page manifest.
- [x] All pages have native text, rendered-page evidence metadata, and extraction-quality status.
- [x] All chunks are revision-scoped, page/citation anchored, and embedded using `text-embedding-3-large` at 3072 dimensions.
- [x] Tables and figures are revision-scoped and retain page, caption/title, image-region evidence, and review status.
- [x] Retrieval cannot mix revisions and cannot use a staging revision through the active-revision path.
- [x] Coverage/retrieval failures are explicit and prevent activation.
- [x] The 2024 corpus and application runtime remain unchanged.

## Implementation Checklist

- [x] Create the ASRS corpus schema migration artifact before database writes.
- [x] Apply the migration to the dedicated ASRS project and verify object/readback state.
- [x] Create a deterministic ingestion command with source hashing and resumable run state.
- [x] Extract all 122 pages and revision-aware chunks.
- [x] Extract tables/figures with rendered-page evidence and review states.
- [x] Generate/store all chunk embeddings and verify dimension/count coverage.
- [x] Add activation and retrieval guardrails plus a verification report.
- [x] Post kickoff, milestones, blockers, and evidence to AAI-1184.

## Integration and Verification

- [x] Targeted unit/static checks pass.
- [x] Live target readback proves page/chunk/table/figure/embedding coverage.
- [x] Representative ASRS requirement queries return only the `2026-04` staging revision when explicitly requested.
- [x] Active-revision retrieval continues to exclude staging data.
- [x] Evidence artifacts and a canonical screenshot are attached to AAI-1184.
- [x] Task-owned files and evidence are published to `origin/main` with exact remote readback.

## Failure-Loudly Contract

- Cause surfaced as: exact page/object/provider failure with revision ID and ingestion-run ID.
- Detection path: coverage verifier compares PDF page manifest, extracted rows, review states, embedding counts/dimensions, and revision status.
- Recovery path: rerun only failed immutable objects by source hash; never activate until all required gates pass.

## Incident Learning

- Failure fingerprint: N/A
- Root cause: N/A
- Detection gap: N/A
- Prevention: revision-scoped uniqueness, source hashes, staging-only retrieval, and activation coverage gates prevent silent mixed-edition answers.
- Guardrail evidence: revision-scoped schema/RPCs, full corpus verifier, reviewed-structure embedding verifier, retrieval checks, and authenticated frontend/chat proof.

## Evidence

| Check | Command / artifact | Result | Notes |
| --- | --- | --- | --- |
| Task setup | This task file | Pass | Full task scope and non-destructive boundary recorded before implementation. |
| PDF review | `pdfinfo` and Appendix B extraction | Pass | 122 pages; April 2026 revision contains material requirements changes. |
| Migration ledger | `supabase --workdir infrastructure/asrs-supabase migration list --db-url "$SUPABASE_ASRS_DATABASE_URL"` | Pass | Local and remote contain `20260720121737` and `20260720122529`. |
| Static check | `python3 -m py_compile scripts/asrs/ingest_fmds0834.py scripts/asrs/verify_fmds0834.py` | Pass | Both commands compile. |
| Live ingestion | `python3 -u scripts/asrs/ingest_fmds0834.py <source-pdf>` | Pass | 122 pages, 225 chunks, 225 embeddings, 58 table occurrences, and 61 initial figure candidates; source review later proved one page-43 table-cell false positive and corrected the canonical inventory to 60 real figures. |
| Live verification | `scripts/asrs/verify_fmds0834.py` | Pass | 123 storage objects; 3072 dimensions on every chunk; explicit staging retrieval only; legacy corpus unchanged. |
| Activation guard | `select activate_fmds_revision(...)` inside a verification block | Pass | Rejected activation: `Table review incomplete: 0 of 58 reviewed`. |
| Evidence | `docs/ops/evidence/2026-07-20-fmds0834-2026/` | Pass | Human-readable and machine-readable coverage reports. |
| Linear milestone | AAI-1184 comment `70fa2984-109d-4752-b439-9202eec3f52c` | Pass | Scope, commands, blockers, next action, and a viewable coverage screenshot are attached. |

## Remaining Risk

- All 58 tables and 60 real figures are now source-reviewed and embedded, but only reviewed deterministic rule cards may produce Verified calculations. A complete head count/configuration remains Pending Review when controlling inputs or reviewed rule coverage are missing.
- The revision remains staging intentionally. Promotion to active is a separate lifecycle decision; the AI chat already selects one eligible revision explicitly and cannot blend editions.
- Exact-file publication is the guardrail for the shared dirty checkout; task-owned artifacts are confirmed on `origin/main` without absorbing unrelated edits.

## Final Status

- [x] All required checklist items are complete.
- [x] Evidence is filled in.
- [x] Incident learning is linked or explicitly N/A.
- [x] Any remaining lifecycle boundary and engineering-coverage risk are explicit.
